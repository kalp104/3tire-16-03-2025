@model PizzaShop.Repository.ModelView.MenuWithItemsViewModel

<div class="modal fade AddItemBox" id="addModifiers" data-bs-backdrop="static" data-bs-keyboard="false" tabindex="-1"
  aria-labelledby="staticBackdropLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="staticBackdropLabel">Add Modifier</h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <form asp-action="AddModifier" asp-controller="Menu" method="post">
        @Html.AntiForgeryToken()
        <div class="modal-body">
          <div class="row">
            <div class="col-6 mb-3 border border rounded ">
              <label class="form-label " style="font-size: 14px; margin-bottom: 0px; margin-top: 6px;">Modifier
                Groups</label>
              <div class="checkbox-container" style="height:100px; overflow-y: scroll;">
                @if (Model.modifiergroups != null && Model.modifiergroups.Any())
                {
                  @foreach (var c in Model.modifiergroups)
                  {
                    <div class="form-check">
                      <input type="checkbox" class="form-check-input" name="modifiersViewModel.Modifiergroupid"
                        value="@c.Modifiergroupid" id="modifiergroup_@c.Modifiergroupid" />
                      <label class="form-check-label" for="modifiergroup_@c.Modifiergroupid">
                        @c.Modifiergroupname
                      </label>
                    </div>
                  }
                }
                else
                {
                  <p>No modifier groups available.</p>
                }
              </div>
              <span asp-validation-for="modifiersViewModel.Modifiergroupid" class="text-danger"></span>
            </div>
            <div class="col-6 form-floating mb-3">
              <input asp-for="modifiersViewModel.Modifiername" type="text" class="form-control"
                placeholder="Enter modifier name" />
              <label>Name</label>
              <span asp-validation-for="modifiersViewModel.Modifiername" class="text-danger"></span>
            </div>
          </div>
          <div class="row">
            <div class="col-4 form-floating mb-3">
              <input asp-for="modifiersViewModel.Modifierrate" type="number" class="form-control" placeholder="₹" />
              <label>Rate</label>
              <span asp-validation-for="modifiersViewModel.Modifierrate" class="text-danger"></span>
            </div>
            <div class="col-4 form-floating mb-3">
              <input type="number" asp-for="modifiersViewModel.Modifierquantity" class="form-control"
                placeholder="Quantity" />
              <label>Quantity</label>
              <span asp-validation-for="modifiersViewModel.Modifierquantity" class="text-danger"></span>
            </div>
            <div class="col-4 form-floating mb-3">
              <select class="form-select" asp-for="modifiersViewModel.Modifierunit">
                <option value="0" selected>Select Units</option>
                <option value="1">Piece</option>
                <option value="2">Kg</option>
              </select>
              <span asp-validation-for="modifiersViewModel.Modifierunit" class="text-danger"></span>
              <label for="floatingSelect">Units</label>
            </div>
          </div>
          <div class="row">
            <div class="col-12 form-floating mb-3">
              <textarea asp-for="modifiersViewModel.Modifierdescription" class="form-control"
                placeholder="Enter modifier description"></textarea>
              <label>Description</label>
              <span asp-validation-for="modifiersViewModel.Modifierdescription" class="text-danger"></span>
            </div>
          </div>
        </div>
        <div class="modal-footer">
          <button type="submit" class="btn btn-primary">Save</button>
          <button type="button" class="btn border-primary text-primary" data-bs-dismiss="modal">Cancel</button>
        </div>
      </form>
    </div>
  </div>
</div>

<!-- Include validation scripts -->
@section Scripts {
  <script src="~/lib/jquery/dist/jquery.min.js"></script>
  <script src="~/lib/jquery-validation/dist/jquery.validate.min.js"></script>
  <script src="~/lib/jquery-validation-unobtrusive/jquery.validate.unobtrusive.min.js"></script>
  <script>
    $(document).ready(function () {
      // Optional: Add custom validation if needed
      $('form').on('submit', function () {
        var checkedCount = $('input[name="modifiersViewModel.Modifiergroupid"]:checked').length;
        if (checkedCount === 0) {
          alert('Please select at least one modifier group.');
          return false; // Prevent form submission
        }
      });
    });
  </script>
}